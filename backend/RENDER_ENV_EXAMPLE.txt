# RENDER ENVIRONMENT VARIABLES REFERENCE
# Copy these variables to your Render web service environment variables

# ==========================================
# CRITICAL VARIABLES (Required)
# ==========================================

# Database Connection
# This is automatically set if you use Render PostgreSQL
# Or set manually for external database
DATABASE_URL=postgresql://username:password@host:port/database_name

# Security
SECRET_KEY=your-random-secret-key-here
DEBUG=False
ALLOWED_HOSTS=srv-d94ldti8qa3s73cuk85g.onrender.com

# ==========================================
# OPTIONAL VARIABLES
# ==========================================

# Redis (for Celery - optional)
REDIS_URL=redis://your-redis-instance:6379/0

# Email Configuration (optional)
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_HOST_USER=apikey
EMAIL_HOST_PASSWORD=your-sendgrid-api-key
DEFAULT_FROM_EMAIL=noreply@yesayaministry.org

# JWT Configuration
ACCESS_TOKEN_LIFETIME_MINUTES=30
REFRESH_TOKEN_LIFETIME_DAYS=7

# Storage (Supabase - optional)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-or-service-key

# Media Files Storage
MEDIA_ROOT=/opt/render/project/backend/media

# Python Version
PYTHON_VERSION=3.10.12

# ==========================================
# NOTES
# ==========================================

# 1. Generate SECRET_KEY using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 2. Replace 'your-app-name.onrender.com' with your actual Render URL
# 3. For production, always set DEBUG=False
# 4. Keep all sensitive data in environment variables only
# 5. Never commit .env file to version control
